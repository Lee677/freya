{
  "name": "freya",
  "private": true,
  "scripts": {
    "deploy": "node tools/build-stamp.mjs && wrangler pages deploy . --project-name=cad"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
